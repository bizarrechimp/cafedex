# Code Citations

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: Mongo
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: Mongo
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: Mongo
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: Mongo
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise:
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise:
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise:
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise:
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<Mongo
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<Mongo
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<Mongo
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<Mongo
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<Mongo
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<Mongo
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {

```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  //
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads cause
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).

```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithM
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis &
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<Mon
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };


```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWith
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongo
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise)
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new Mongo
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    global
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise
```

## License: MIT

https://github.com/vercel/next.js/blob/42f8ac16c6ff497f297198e42fc2abb113bf4a7b/examples/with-mongodb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```

## License: unknown

https://github.com/Master-Git-Hack/Cotizacion/blob/442be08040d05282880cc832915463281258c591/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```

## License: unknown

https://github.com/Jacksonmills/combo-z/blob/fddfebcecd23d0691a291bd24c3c2e304e2e5cc8/src/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```

## License: unknown

https://github.com/bstephen1/iron-log/blob/04ee743e7447065db6078aa7e5da5a9d8a6e3dfc/lib/backend/mongoConnect.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```

## License: unknown

https://github.com/lekhanh240895/netflix-ui/blob/20e353aabf58ed93bfa7ea52769c0e7446a798cb/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```

## License: unknown

https://github.com/Tagaca19e/verifai/blob/864e5d74e3ad643583a48759a46bb2fb40272c11/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```

## License: unknown

https://github.com/SamAmann/template_next-app/blob/85b17f797c33356afa95b1ac5d08555bfbffcf60/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```

## License: unknown

https://github.com/gianlucadonato/gspace/blob/2fa3944dc49d3382619ae93dc43d28a22be9ed4c/lib/mongodb.ts

```
;

let client: MongoClient;
let clientPromise: Promise<MongoClient>;

if (process.env.NODE_ENV === 'development') {
  // In development mode, use a global variable so that the value
  // is preserved across module reloads caused by HMR (Hot Module Replacement).
  let globalWithMongo = global as typeof globalThis & {
    _mongoClientPromise?: Promise<MongoClient>;
  };

  if (!globalWithMongo._mongoClientPromise) {
    client = new MongoClient(process.env.MONGODB_URI, options);
    globalWithMongo._mongoClientPromise = client.connect
```
